/**
 * Index file for React hooks
 *
 * @author Richard Nguyen <richard.ng0616@gmail.com>
 */

import useLocalStorage from "./useLocalStorage";

const exported = Object.assign("hook", {
  useLocalStorage,
});

export default exported;
